# --- Database ---
POSTGRES_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME

# --- JWT ---
JWT_SECRET=change-me
JWT_ALG=HS256
JWT_EXPIRE_MINUTES=120

# --- Mongo ---
MONGO_URL=mongodb+srv://USER:PASSWORD@HOST/?retryWrites=true&w=majority
MONGO_DB=vite_gourmand

# --- Email : Mailgun API ---
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=your-mailgun-domain.mailgun.org
MAILGUN_FROM=Vite & Gourmand <postmaster@your-domain.mailgun.org>

# --- SMTP (déprécié, remplacé par Mailgun API) ---
# SMTP_SERVER=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=your-smtp-username
# SMTP_PASSWORD=your-smtp-password
# SMTP_USE_TLS=true
# SMTP_FROM="Vite & Gourmand <no-reply@vitegourmand.test>"

# --- Email tokens ---
EMAIL_CONFIRMATION_EXPIRE_HOURS=24
EMAIL_CONFIRMATION_SECRET=change-me-too

# --- Front ---
FRONTEND_URL=https://vitegourmand.netlify.app

# --- API ---
ENVIRONMENT=development
API_TITLE=Vite & Gourmand API
API_VERSION=1.0.0
